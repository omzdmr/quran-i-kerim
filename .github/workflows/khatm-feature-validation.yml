name: Khatm Feature Validation

on:
  push:
    branches:
      - manual/khatm-yearly-target

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.47.2'
          channel: stable
          cache: true

      - name: Fetch bundled content
        run: python3 tool/fetch_content.py

      - name: Get dependencies
        run: flutter pub get

      - name: Generate localizations
        run: flutter gen-l10n

      - name: Format changed code
        run: |
          dart format \
            lib/src/features/plans/reading_plan_store.dart \
            lib/src/features/plans/plans_screen.dart \
            lib/src/l10n/strings/plan_strings.dart \
            test/reading_plan_store_test.dart

      - name: Persist formatter output
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            lib/src/features/plans/reading_plan_store.dart \
            lib/src/features/plans/plans_screen.dart \
            lib/src/l10n/strings/plan_strings.dart \
            test/reading_plan_store_test.dart \
            test/plans_screen_compile_test.dart
          if ! git diff --cached --quiet; then
            git commit -m "style: format khatm feature"
            git push
          fi

      - name: Test reading plan store
        run: timeout 90s flutter test test/reading_plan_store_test.dart --reporter expanded

      - name: Test plan localization parity
        run: timeout 90s flutter test test/plan_strings_test.dart --reporter expanded

      - name: Compile plans screen
        run: timeout 90s flutter test test/plans_screen_compile_test.dart --reporter expanded
