name: Apply Audio v2 complete

on:
  push:
    branches: [ feature/localization-v01 ]
    paths:
      - '.github/workflows/apply-audio-v2-complete.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.47.2'
          channel: stable
          cache: true

      - name: Apply Audio v2 completion
        run: python3 tool/apply_audio_v2_complete.py

      - name: Remove helper
        run: rm tool/apply_audio_v2_complete.py

      - name: Fetch bundled content
        run: python3 tool/fetch_content.py

      - name: Resolve dependencies
        run: flutter pub get

      - name: Format
        run: dart format lib test

      - name: Analyze
        run: flutter analyze lib --no-fatal-infos

      - name: Run tests
        run: flutter test

      - name: Commit source changes
        run: |
          git config user.name 'omzdmr'
          git config user.email '154471764+omzdmr@users.noreply.github.com'
          git add pubspec.yaml pubspec.lock lib test tool/apply_audio_v2_complete.py
          git add -u
          git commit -m 'Complete Audio v2 downloads and playback controls'
          git push
